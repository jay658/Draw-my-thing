type LinesT = {
  tool: string
  points: number[],
  stroke: string,
  strokeWidth: string
}

type SettingsT = {
  tool: string,
  stroke: string,
  strokeWidth: string
}

export type {
  LinesT,
  SettingsT,
}